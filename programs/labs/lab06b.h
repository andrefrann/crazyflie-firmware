#include "mbed.h"
#include "crazyflie.h"

Mixer mixer;

int main()
{
    // wait(8);
    // mixer.arm();
    // mixer.actuate(0.5*m*g,0,0,0);
    // wait(5);
    // mixer.disarm();

    while(true)
    {
    }
}   